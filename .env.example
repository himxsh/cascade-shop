# Postgres (docker compose maps host 5433 → container 5432)
DATABASE_URL=postgresql://shop:shop@localhost:5433/cascade_shop

# DataHub GMS — local for laptop; HTTPS tunnel URL for GitHub Actions
DATAHUB_GMS_URL=http://localhost:8080
DATAHUB_TOKEN=

# Cascade Act (set on GitHub Actions secrets for live path)
# DATAHUB_GMS_URL=https://<your-tunnel>
# LLM_API_KEY=
# CASCADE_WRITEBACK=1